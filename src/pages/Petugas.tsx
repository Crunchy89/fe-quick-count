import { useEffect } from "react";
const Petugas = () => {
    useEffect(()=>{
        console.log("dashboard i loaded")
    },[])
    return (
        <div>
            petugas
        </div>
    );
};

export default Petugas;